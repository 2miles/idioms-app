# Environment variables for local E2E testing

# URL of the test backend server (typically started on a different port than dev)
# ex: http://localhost:3010
VITE_API_BASE_URL=

# Environment flag used to display the TEST banner in the UI
VITE_APP_ENV=test