# Example environment variables for production Express server
# These should be added to your deployment platform (e.g. Railway) — not used locally

# Auth0 production credentials
AUTH0_AUDIENCE=https://api.idiomvault.com
AUTH0_ISSUER_BASE_URL=https://your-auth0-tenant-domain.auth0.com/

# Frontend origin (e.g. Vercel or Netlify)
CORS_ORIGIN=https://your-frontend.example.com

# Production database connection string (e.g. Supabase or Railway)
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>