# Environment variables for running the Express test server (used in E2E testing)

# Set the Node.js environment mode
NODE_ENV=test

# Port for the test server to run on
PORT=3010

# Frontend origin allowed to access the test server
# Use 5174 if your test client runs separately from dev (5173)
CORS_ORIGIN=http://localhost:5174

# Auth0 test credentials (replace with your own tenant info)
AUTH0_AUDIENCE=https://your-api-audience.example.com
AUTH0_ISSUER_BASE_URL=https://your-auth0-tenant.example.com/

# Test DB connection string
DATABASE_URL_TEST=postgresql://<user>:<password>@<host>:<port>/<database>