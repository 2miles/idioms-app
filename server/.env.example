# Environment variables for running the Express server in local development

# Port for the local development server to run on
PORT=

# Frontend origin allowed to access the local server (e.g. http://localhost:5173)
CORS_ORIGIN=

# Auth0 credentials for local development (replace with your own tenant info)
AUTH0_AUDIENCE=
AUTH0_ISSUER_BASE_URL=

# Database connection string for local development
DATABASE_URL_DEV=postgresql://<user>:<password>@<host>:<port>/<database>